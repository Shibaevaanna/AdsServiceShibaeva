<Page x:Class="AdsServiceShibaeva.Pages.UserAdsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Мои объявления">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="AddButton" Content="Добавить объявление" Style="{StaticResource button}" 
                    Width="150" Height="30" Click="AddButton_Click" Margin="5"/>
            <Button x:Name="BackButton" Content="Назад" Style="{StaticResource button}" 
                    Width="80" Height="30" Click="BackButton_Click" Margin="5"/>
        </StackPanel>

        <!-- Список объявлений -->
        <ListView x:Name="AdsListView" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Заголовок" DisplayMemberBinding="{Binding ad_title}" Width="200"/>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding price, StringFormat={}{0:C}}" Width="100"/>
                    <GridViewColumn Header="Город" DisplayMemberBinding="{Binding cities.city_name}" Width="120"/>
                    <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding categories.category_name}" Width="120"/>
                    <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding ad_types.type_name}" Width="100"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding ad_statuses.status_name}" Width="100"/>
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding post_date, StringFormat=dd.MM.yyyy}" Width="100"/>
                </GridView>
            </ListView.View>

            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Редактировать" Click="EditMenuItem_Click"/>
                    <MenuItem Header="Удалить" Click="DeleteMenuItem_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <TextBlock x:Name="NoAdsText" Grid.Row="1" Text="У вас пока нет объявлений" 
                   Style="{StaticResource header}" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>