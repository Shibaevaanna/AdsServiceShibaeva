<Page x:Class="AdsServiceShibaeva.Pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Главная">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">

            <StackPanel Orientation="Horizontal">
                <Image Source="/images/Лого.png" Width="100" Height="100"/>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="Поиск по названию" Style="{StaticResource text}"/>
                <TextBox x:Name="titlefilter" Width="150" Height="25" TextChanged="titlefilter_TextChanged"/>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="Сортировка" Style="{StaticResource text}"/>
                <ComboBox x:Name="sortcombobox" Width="150" Height="25" SelectedIndex="0" SelectionChanged="sortcombobox_SelectionChanged">
                    <ComboBoxItem Content="По городу"/>
                    <ComboBoxItem Content="По категории"/>
                    <ComboBoxItem Content="По типу"/>
                    <ComboBoxItem Content="По статусу"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="Действия" Style="{StaticResource text}"/>
                <Button x:Name="clearFilter" Style="{StaticResource button}" Content="Очистить фильтр" Width="120" Height="25" Click="clearFilter_Click"/>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="Войти в аккаунт" Style="{StaticResource text}"/>
                <Button x:Name="authButton" Style="{StaticResource button}" Content="Войти" Width="120" Height="25" Click="authButton_Click"/>
            </StackPanel>
        </StackPanel>

        <ListView x:Name="listview_ads" Grid.Row="1" HorizontalContentAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10" Width="220">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="{Binding ad_title}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource header}"/>
                        <TextBlock Grid.Row="1" Text="{Binding ad_description}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="2" Text="{Binding price}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="3" Text="{Binding post_date, StringFormat=yyyy-MM-dd}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="4" Text="{Binding cities.city_name}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="5" Text="{Binding categories.category_name}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="6" Text="{Binding ad_types.type_name}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <TextBlock Grid.Row="7" Text="{Binding ad_statuses.status_name}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource ad_text}"/>
                        <Image Grid.Row="8" Stretch="UniformToFill" Width="200" HorizontalAlignment="Center">
                            <Image.Source>
                                <Binding Path="photo">
                                    <Binding.TargetNullValue>
                                        <BitmapImage UriSource="/images/image.png"/>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
